[package]
name = "key-stream"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.51.0", features = ["sync", "rt"] }

[dev-dependencies]
tokio = { version = "1.51.0", features = ["macros", "rt"] }
